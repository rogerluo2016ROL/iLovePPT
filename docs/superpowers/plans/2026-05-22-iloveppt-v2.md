# iLovePPT v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 iLovePPT 从"skill/app 骑墙"重构为诚实的纯 skill：机械部分 = `build.py`(`deck_plan.json → .pptx + PNG`)，智能部分 = Claude 照文档流程做，二者用 `deck_plan.json` 接缝隔开；引入 `layout.py` 几何原语修掉固定坐标/留白问题；建 `evals/` 回归集。

**Architecture:** 三层——`skills/pptx/helpers.py`(pptx 原语 + 设计 token SSOT) + `skills/pptx/layout.py`(几何原语) → `skills/pptx-deck/themes/tech_blue.py`(make_* 薄组合) → `skills/pptx-deck/build.py`(消费 deck_plan.json)。

**Tech Stack:** Python 3.10+（python-pptx、lxml、pyyaml）、pytest、LibreOffice(soffice)、poppler(pdftoppm)。

**Spec source of truth:** `docs/superpowers/specs/2026-05-22-iloveppt-v2-design.md`。Plan 给新接口的完整代码；对 `tech_blue.py` 重写给模式 + 代表函数 + 逐函数映射表。

**工作目录:** `/Users/pc2026/Documents/DevTools/KillPPTs/`

---

## File Structure

```
skills/pptx/layout.py            新增——几何原语
skills/pptx/helpers.py           不变
skills/pptx-deck/build.py        新增（由 workflow.py 改造）
skills/pptx-deck/workflow.py     删除
skills/pptx-deck/themes/tech_blue.py   重写 make_*
tests/pptx/test_layout.py        新增——几何单测
tests/pptx_deck/test_tech_blue.py      重写（compare/cards 改名）
tests/pptx_deck/test_build.py    新增（由 test_workflow.py 改造）
tests/pptx_deck/test_workflow.py 删除
evals/                           新增——回归集
skills/pptx-deck/template-extract.md   由 template-ingest.md 改名
skills/pptx-deck/workflow.md     重写为 Claude 流程
（README / CLAUDE.md / 各 SKILL.md / USAGE / MANUAL / diagram-planning / visual-qa / content-writing 更新）
```

---

# Phase A+B —— 代码重构

## Task 1：`layout.py` 几何原语

**Files:**
- Create: `skills/pptx/layout.py`
- Test: `tests/pptx/test_layout.py`

- [ ] **Step 1: 写失败测试**

`tests/pptx/test_layout.py`：

```python
"""layout.py 几何原语单测——纯函数,确定性,断言坐标。"""
from pptx.util import Emu, Inches
import layout as L


def test_box_fields():
    b = L.Box(x=Inches(1), y=Inches(2), w=Inches(3), h=Inches(4))
    assert b.x == Inches(1) and b.w == Inches(3)


def test_content_region_inside_slide():
    r = L.content_region()
    assert r.x > 0 and r.y > 0
    assert r.x + r.w <= Inches(13.333) + 1
    assert r.y + r.h <= Inches(7.5) + 1


def test_columns_count_and_widths_equal():
    box = L.Box(Inches(0), Inches(0), Inches(12), Inches(6))
    cols = L.columns(box, 3, gap=Inches(0))
    assert len(cols) == 3
    assert cols[0].w == cols[1].w == cols[2].w
    # 无 gap 时三列总宽 = box.w
    assert abs((cols[0].w + cols[1].w + cols[2].w) - Inches(12)) < 100


def test_columns_respects_gap_and_order():
    box = L.Box(Inches(0), Inches(0), Inches(10), Inches(6))
    cols = L.columns(box, 2, gap=Inches(1))
    assert cols[0].x == Inches(0)
    assert cols[1].x > cols[0].x + cols[0].w  # 第二列在第一列右侧,留了 gap


def test_rows_count_and_heights_equal():
    box = L.Box(Inches(0), Inches(0), Inches(12), Inches(6))
    rs = L.rows(box, 3, gap=Inches(0))
    assert len(rs) == 3
    assert rs[0].h == rs[1].h == rs[2].h
    assert rs[1].y > rs[0].y


def test_stack_middle_centers_group():
    box = L.Box(Inches(0), Inches(0), Inches(10), Inches(10))
    boxes = L.stack(box, [Inches(2), Inches(2)], gap=Inches(0), align="middle")
    # 两块各 2",共 4",在 10" 内居中 → 顶部留 3"
    assert abs(boxes[0].y - Inches(3)) < 100


def test_stack_top_starts_at_box_top():
    box = L.Box(Inches(0), Inches(1), Inches(10), Inches(10))
    boxes = L.stack(box, [Inches(2)], gap=Inches(0), align="top")
    assert boxes[0].y == Inches(1)


def test_split_ratio():
    box = L.Box(Inches(0), Inches(0), Inches(10), Inches(6))
    left, right = L.split(box, 0.4, gap=Inches(0))
    assert abs(left.w - Inches(4)) < 100
    assert abs(right.w - Inches(6)) < 100
    assert right.x >= left.x + left.w


def test_inset_shrinks_box():
    box = L.Box(Inches(1), Inches(1), Inches(10), Inches(8))
    inner = L.inset(box, Inches(0.5), Inches(0.5))
    assert inner.x == Inches(1.5)
    assert inner.w == Inches(9)
```

- [ ] **Step 2: 跑测试验证 fail**

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
python3 -m pytest tests/pptx/test_layout.py -v
```

期望：collection error 或 9 FAIL（`ModuleNotFoundError: layout`）。

- [ ] **Step 3: 写 `skills/pptx/layout.py`**

```python
"""iLovePPT 几何原语 —— 主题无关的区域切分。

Box 是 (x, y, w, h),EMU 单位。原语把一个 Box 切成子 Box,供 theme 的
make_* 函数定位内容。纯函数、无 pptx 渲染依赖,可确定性单测。
"""
from dataclasses import dataclass

from pptx.util import Emu, Inches, Length

import helpers as H


@dataclass(frozen=True)
class Box:
    """一块矩形区域。x/y/w/h 均为 EMU（python-pptx Length）。"""
    x: Length
    y: Length
    w: Length
    h: Length


def content_region() -> Box:
    """header 与 footer 之间的内容区（基于 helpers.py 的边距常量）。"""
    return Box(
        x=H.LEFT_MARGIN,
        y=H.HEADER_BOTTOM,
        w=Emu(H.SLIDE_W - H.LEFT_MARGIN - H.RIGHT_MARGIN),
        h=Emu(H.FOOTER_TOP - H.HEADER_BOTTOM),
    )


def columns(box: Box, n: int, gap: Length = Inches(0.3)) -> list[Box]:
    """把 box 横切成 n 等宽列,列间留 gap。"""
    col_w = Emu(int((box.w - gap * (n - 1)) / n))
    return [
        Box(x=Emu(box.x + i * (col_w + gap)), y=box.y, w=col_w, h=box.h)
        for i in range(n)
    ]


def rows(box: Box, n: int, gap: Length = Inches(0.2)) -> list[Box]:
    """把 box 纵切成 n 等高行,行间留 gap。"""
    row_h = Emu(int((box.h - gap * (n - 1)) / n))
    return [
        Box(x=box.x, y=Emu(box.y + i * (row_h + gap)), w=box.w, h=row_h)
        for i in range(n)
    ]


def stack(box: Box, heights: list[Length], gap: Length = Inches(0.2),
          align: str = "middle") -> list[Box]:
    """按给定块高纵向排布,整组在 box 内对齐。align: top|middle|bottom。"""
    total = sum(heights) + gap * (len(heights) - 1)
    if align == "top":
        cur = box.y
    elif align == "bottom":
        cur = box.y + box.h - total
    else:  # middle
        cur = box.y + (box.h - total) // 2
    out: list[Box] = []
    for hgt in heights:
        out.append(Box(x=box.x, y=Emu(int(cur)), w=box.w, h=hgt))
        cur = cur + hgt + gap
    return out


def split(box: Box, ratio: float, gap: Length = Inches(0.3)) -> tuple[Box, Box]:
    """按 ratio 把 box 横切成左右两块（ratio = 左块占可用宽的比例）。"""
    left_w = Emu(int((box.w - gap) * ratio))
    right_w = Emu(box.w - gap - left_w)
    left = Box(x=box.x, y=box.y, w=left_w, h=box.h)
    right = Box(x=Emu(box.x + left_w + gap), y=box.y, w=right_w, h=box.h)
    return left, right


def inset(box: Box, dx: Length, dy: Length) -> Box:
    """四周各内缩 dx（左右）/ dy（上下）。"""
    return Box(
        x=Emu(box.x + dx), y=Emu(box.y + dy),
        w=Emu(box.w - 2 * dx), h=Emu(box.h - 2 * dy),
    )
```

注：`pptx.util.Length` 是 `int` 子类,算术结果退化为 `int`,故结果统一 `Emu(int(...))` 包回。`layout` 通过 `pyproject.toml` 的 `pythonpath`（含 `skills/pptx`）可直接 `import helpers`。

- [ ] **Step 4: 跑测试验证 pass**

```bash
python3 -m pytest tests/pptx/test_layout.py -v
```

期望：9 PASS。

- [ ] **Step 5: commit**

```bash
git add skills/pptx/layout.py tests/pptx/test_layout.py
git commit -m "feat(pptx): add layout.py geometry primitives"
```

---

## Task 2：重写 `tech_blue.py` 的 make_* 为 layout 组合

**Files:**
- Modify: `skills/pptx-deck/themes/tech_blue.py`（重写 11 个 make_* 函数体；色板/字体别名段不动）
- Modify: `tests/pptx_deck/test_tech_blue.py`（`two_col_compare`/`three_col_cards` → `compare`/`cards`）

**说明**：`tech_blue.py` 顶部的 import、字体/色板 SSOT 别名段、`_blank_slide`/`_add_title` 辅助保留。重写 11 个 `make_*` 函数,改用 `layout.py` 原语定位。spec §4.2 给了逐函数映射。

- [ ] **Step 1: 加 layout 模块 import**

在 `tech_blue.py` 的 `import helpers as H` 下一行加：

```python
import layout as L
```

- [ ] **Step 2: 加文本辅助 `_text`**

代表函数会用到 `_text(slide, box, text, *, size, bold, color)` —— 在 `_add_title` 下面加（**先加这个,后面的函数才能用**）：

```python
def _text(slide: Slide, box: "L.Box", text: str, *, size: int,
          bold: bool = False, color=None, align=PP_ALIGN.LEFT) -> None:
    """在一个 Box 内放一段文字（textbox + margin 归零 + set_font）。"""
    if color is None:
        color = H.GRAY_900
    tb = slide.shapes.add_textbox(box.x, box.y, box.w, box.h)
    H.fix_textbox_margins(tb.text_frame)
    tb.text_frame.word_wrap = True
    p = tb.text_frame.paragraphs[0]
    p.alignment = align
    r = p.add_run()
    r.text = text
    H.set_font(r, name=FONT_HEADER, size=size, bold=bold, color=color)
```

- [ ] **Step 3: 重写 4 个代表函数（覆盖 4 种模式）**

模式 A — 全屏背景（cover / closing / section_divider 用）。`make_cover`：

```python
def make_cover(prs: _Pres, title: str, subtitle: str) -> Slide:
    s = _blank_slide(prs)
    H.rect(s, 0, 0, H.SLIDE_W, H.SLIDE_H, PRIMARY_DEEP)
    region = L.content_region()
    blocks = L.stack(region, [Inches(1.2), Inches(0.7)], gap=Inches(0.3),
                     align="middle")
    _text(s, blocks[0], title, size=44, bold=True, color=H.WHITE)
    _text(s, blocks[1], subtitle, size=20, color=PRIMARY_TINT)
    return s
```

模式 B — N 列（cards / compare 用）。`make_cards`：

```python
def make_cards(prs: _Pres, title: str, cards: list[dict[str, str]]) -> Slide:
    s = _blank_slide(prs)
    _add_title(s, title, size=28)
    cols = L.columns(L.content_region(), len(cards))
    for col, card in zip(cols, cards):
        H.card(s, col.x, col.y, col.w, col.h, fill=H.WHITE,
               border=H.GRAY_300, accent=PRIMARY)
        inner = L.inset(col, Inches(0.3), Inches(0.25))
        parts = L.stack(inner, [Inches(0.5), Inches(2.0)], gap=Inches(0.15),
                        align="top")
        _text(s, parts[0], card["title"], size=18, bold=True, color=PRIMARY_DEEP)
        _text(s, parts[1], card["body"], size=13, color=H.GRAY_900)
    return s
```

模式 C — 纵向堆叠居中（bullet_list / single_focus 用）。`make_bullet_list`：

```python
def make_bullet_list(prs: _Pres, title: str, items: list[str]) -> Slide:
    s = _blank_slide(prs)
    _add_title(s, title, size=28)
    region = L.content_region()
    line_h = Inches(0.5)
    block = L.stack(region, [Emu(line_h * len(items))], align="middle")[0]
    H.bullets(s, block.x, block.y, block.w, block.h, items=items, size=16,
              accent_color=PRIMARY, body_color=H.GRAY_900)
    return s
```

模式 D — 左右分栏（pic_text 用）。`make_pic_text`：

```python
def make_pic_text(prs: _Pres, title: str, image_path: str,
                  points: list[dict[str, str]]) -> Slide:
    s = _blank_slide(prs)
    _add_title(s, title, size=28)
    left, right = L.split(L.content_region(), 0.42)
    H.embed_picture(s, image_path, left.x, left.y, height=left.h)
    rboxes = L.rows(right, len(points))
    for rb, p in zip(rboxes, points):
        H.card(s, rb.x, rb.y, rb.w, rb.h, fill=H.WHITE,
               border=H.GRAY_300, accent=PRIMARY)
        inner = L.inset(rb, Inches(0.25), Inches(0.12))
        parts = L.stack(inner, [Inches(0.35), Inches(0.4)], gap=Inches(0.05),
                        align="top")
        _text(s, parts[0], p["title"], size=14, bold=True, color=PRIMARY_DEEP)
        _text(s, parts[1], p["body"], size=11, color=H.GRAY_700)
    return s
```

- [ ] **Step 4: 重写剩余 7 个函数**

按 spec §4.2 + 上述模式重写。逐函数映射：

| 函数 | 签名 | 模式 / 原语 |
|---|---|---|
| `make_toc(prs, sections)` | `sections: list[str]` | `rows(content_region(), len(sections))`,每行编号 + 标题 |
| `make_section_divider(prs, num, title)` | 不变 | 沿用 `H.section_header`,无需 layout 原语 |
| `make_single_focus(prs, *, big_text, big_number, explanation)` | 不变 | `stack(content_region(), [大数字高, 大字高, 解释高], align="middle")` |
| `make_compare(prs, title, items)` | `items: list[{title, body}]` | 同 `make_cards` 模式（`columns`),卡片交替 accent=PRIMARY/ACCENT |
| `make_table(prs, title, headers, rows)` | 不变 | `H.table_modern` 定位到 `content_region()` 的 x/y/w |
| `make_summary(prs, conclusions, title="核心结论")` | 不变 | `rows(content_region(), len(conclusions))`,每行序号块 + 文字 |
| `make_closing(prs, subtitle="")` | 不变 | 模式 A（全屏背景 + `stack` 居中"谢谢" + subtitle）|

`make_compare` 完整签名：`def make_compare(prs: _Pres, title: str, items: list[dict[str, str]]) -> Slide:`，函数体同 `make_cards` 但 accent 交替：`accent=PRIMARY if i % 2 == 0 else ACCENT`。

`make_two_col_compare` 与 `make_three_col_cards` 函数**删除**。

- [ ] **Step 5: 重写 `test_tech_blue.py`**

把 `test_make_two_col_compare` 改为 `test_make_compare`、`test_make_three_col_cards` 改为 `test_make_cards`，并测多种数量：

```python
def test_make_compare_two_items():
    prs = _new()
    T.make_compare(prs, "对比", items=[
        {"title": "现状", "body": "手工"},
        {"title": "目标", "body": "自动"}])
    assert len(prs.slides) == 1

def test_make_compare_three_items():
    prs = _new()
    T.make_compare(prs, "三方对比", items=[
        {"title": "A", "body": "x"}, {"title": "B", "body": "y"},
        {"title": "C", "body": "z"}])
    assert len(prs.slides) == 1

def test_make_cards_two():
    prs = _new()
    T.make_cards(prs, "两栏", cards=[
        {"title": "卡1", "body": "正文1"}, {"title": "卡2", "body": "正文2"}])
    assert len(prs.slides) == 1

def test_make_cards_four():
    prs = _new()
    T.make_cards(prs, "四栏", cards=[
        {"title": f"卡{i}", "body": f"正文{i}"} for i in range(1, 5)])
    assert len(prs.slides) == 1
```

其余 9 个 `test_make_*`（cover/toc/section_divider/single_focus/bullet_list/table/pic_text/summary/closing）保留,确认签名未变。`test_font_default_is_microsoft_yahei` 保留。

- [ ] **Step 6: 跑测试验证 pass**

```bash
python3 -m pytest tests/pptx_deck/test_tech_blue.py -v
```

期望：14 PASS（9 个不变 make_* + compare×2 + cards×2 + font = 14）。

- [ ] **Step 7: 视觉 smoke**

写 `/tmp/tb_v2.py` 逐个调 11 个 layout（cards 用 4 张、compare 用 3 项、bullet_list 用 3 项验证留白居中）生成 .pptx，`soffice` 渲染，`pdftoppm` 出 PNG，逐张 Read 检查：内容不再顶端挤、卡片数正确、无重叠/溢出。

- [ ] **Step 8: commit**

```bash
git add skills/pptx-deck/themes/tech_blue.py tests/pptx_deck/test_tech_blue.py
git commit -m "feat(pptx-deck): rewrite tech_blue layouts on layout.py primitives"
```

---

## Task 3：`build.py` 机械构建器

**Files:**
- Create: `skills/pptx-deck/build.py`
- Delete: `skills/pptx-deck/workflow.py`
- Create: `tests/pptx_deck/test_build.py`
- Delete: `tests/pptx_deck/test_workflow.py`

- [ ] **Step 1: 写 `build.py`**

```python
"""iLovePPT build.py —— 机械构建器：deck_plan.json → .pptx + PNG。

用法：python3 build.py deck_plan.json [--no-render]

deck_plan.json schema：{theme, output, slides: [{layout, ...fields}, ...]}
智能部分（brief→deck_plan、视觉自检）由 Claude 按文档流程做,不在本文件。
"""
import sys
import json
import shutil
import subprocess
from pathlib import Path
from types import ModuleType
from typing import Any

from pptx import Presentation

HERE = Path(__file__).parent
for _p in [str(HERE.parent / "pptx"), str(HERE)]:
    if _p not in sys.path:
        sys.path.insert(0, _p)

import helpers as H
from themes import tech_blue as _tech_blue

THEMES: dict[str, ModuleType] = {"tech_blue": _tech_blue}


# ----- load_plan -----

def load_plan(path: str | Path) -> dict[str, Any]:
    """读 + 校验 deck_plan.json。记录 _plan_dir 供相对 output 解析。"""
    p = Path(path)
    data = json.loads(p.read_text(encoding="utf-8"))
    for field in ("theme", "output", "slides"):
        if field not in data:
            raise ValueError(f"deck_plan 缺字段: {field}")
    if not isinstance(data["slides"], list) or not data["slides"]:
        raise ValueError("deck_plan.slides 必须是非空 list")
    for i, slide in enumerate(data["slides"], 1):
        if "layout" not in slide:
            raise ValueError(f"deck_plan 第 {i} 页缺 layout 字段")
    data["_plan_dir"] = str(p.resolve().parent)
    return data


# ----- load_theme -----

def _extract_theme_from_pptx(pptx_path: str) -> ModuleType:
    """从用户 .pptx 提取主色与字体,派生临时主题模块。

    从 tech_blue.py 源码加载全新模块实例,再用提取的 token 覆盖
    FONT_* / PRIMARY 属性。token 提取 best-effort,未提取到的保留默认。
    """
    import importlib.util
    tokens = _extract_design_tokens(pptx_path)
    import themes.tech_blue as base_module
    base_path = Path(base_module.__file__)
    out_name = f"extracted_{Path(pptx_path).stem}"
    spec = importlib.util.spec_from_file_location(out_name, base_path)
    if spec is None or spec.loader is None:
        raise RuntimeError(f"无法从 {base_path} 加载主题")
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    if "font_header" in tokens:
        mod.FONT_HEADER = tokens["font_header"]
        mod.FONT_BODY = tokens.get("font_body", tokens["font_header"])
    if "primary" in tokens:
        mod.PRIMARY = tokens["primary"]
    print(f"  从模板提取主题: {out_name}（主色 {tokens.get('primary','默认')}）")
    return mod


def _extract_design_tokens(pptx_path: str) -> dict[str, Any]:
    """从 .pptx 提取主色(accent1)与中文字体(master ea typeface)。best-effort。"""
    from lxml import etree
    tokens: dict[str, Any] = {}
    try:
        prs = Presentation(pptx_path)
    except Exception:
        return tokens
    try:
        from pptx.oxml.ns import qn
        if prs.slide_masters:
            done = False
            for ph in prs.slide_masters[0].placeholders:
                for para in ph.text_frame.paragraphs:
                    for run in para.runs:
                        rPr = run._r.find(qn("a:rPr"))
                        if rPr is not None:
                            ea = rPr.find(qn("a:ea"))
                            if ea is not None and ea.get("typeface"):
                                tokens["font_header"] = ea.get("typeface")
                                tokens["font_body"] = ea.get("typeface")
                                done = True
                                break
                    if done:
                        break
                if done:
                    break
    except Exception:
        pass
    try:
        from pptx.dml.color import RGBColor
        for part in prs.part.package.iter_parts():
            pn = part.partname
            if "theme" in pn and pn.endswith(".xml"):
                root = etree.fromstring(part.blob)
                ns = {"a": "http://schemas.openxmlformats.org/drawingml/2006/main"}
                accent1 = root.find(".//a:accent1//a:srgbClr", ns)
                if accent1 is not None:
                    hx = accent1.get("val", "")
                    if len(hx) == 6:
                        tokens["primary"] = RGBColor(
                            int(hx[0:2], 16), int(hx[2:4], 16), int(hx[4:6], 16))
                        break
    except Exception:
        pass
    return tokens


def load_theme(theme_id: str) -> ModuleType:
    if theme_id in THEMES:
        return THEMES[theme_id]
    if str(theme_id).endswith(".pptx"):
        if not Path(theme_id).exists():
            raise FileNotFoundError(f"theme .pptx 不存在: {theme_id}")
        return _extract_theme_from_pptx(theme_id)
    raise ValueError(f"未知 theme: {theme_id}")


# ----- build_deck -----

def build_deck(plan: dict[str, Any]) -> Path:
    """按 deck_plan 逐 slide 调 make_*,存 .pptx,返回输出路径。"""
    theme = load_theme(plan["theme"])
    prs = Presentation()
    prs.slide_width = H.SLIDE_W
    prs.slide_height = H.SLIDE_H
    for i, slide in enumerate(plan["slides"], 1):
        layout = slide["layout"]
        fn = getattr(theme, f"make_{layout}", None)
        if fn is None:
            raise ValueError(f"第 {i} 页未知 layout: {layout}（theme 无 make_{layout}）")
        fields = {k: v for k, v in slide.items() if k != "layout"}
        try:
            fn(prs, **fields)
        except TypeError as e:
            raise ValueError(f"第 {i} 页 layout={layout}: {e}") from e
    out = Path(plan["output"]).expanduser()
    if not out.is_absolute():
        out = (Path(plan["_plan_dir"]) / out).resolve()
    out.parent.mkdir(parents=True, exist_ok=True)
    prs.save(str(out))
    return out


# ----- render -----

def render(pptx_path: str | Path, out_dir: str | Path) -> list[Path]:
    """soffice → PDF → pdftoppm → 逐页 PNG。返回 PNG 路径列表。"""
    if shutil.which("soffice") is None:
        raise RuntimeError("soffice 未安装。请: brew install --cask libreoffice")
    if shutil.which("pdftoppm") is None:
        raise RuntimeError("pdftoppm 未安装。请: brew install poppler")
    pptx_path = Path(pptx_path)
    out_dir = Path(out_dir)
    out_dir.mkdir(parents=True, exist_ok=True)
    try:
        subprocess.run(
            ["soffice", "--headless", "--convert-to", "pdf",
             str(pptx_path), "--outdir", str(out_dir)],
            check=True, capture_output=True, text=True)
    except subprocess.CalledProcessError as e:
        raise RuntimeError(f"soffice 转 PDF 失败: {e.stderr}") from e
    pdf = out_dir / (pptx_path.stem + ".pdf")
    if not pdf.exists():
        raise RuntimeError(f"soffice 跑了但未产 PDF: {pdf}")
    try:
        subprocess.run(
            ["pdftoppm", "-jpeg", "-r", "120", str(pdf), str(out_dir / "page")],
            check=True, capture_output=True, text=True)
    except subprocess.CalledProcessError as e:
        raise RuntimeError(f"pdftoppm 转 PNG 失败: {e.stderr}") from e
    return sorted(out_dir.glob("page-*.jpg"))


# ----- CLI -----

def main(argv: list[str]) -> None:
    if not argv:
        sys.exit("用法: python3 build.py deck_plan.json [--no-render]")
    plan_path = argv[0]
    do_render = "--no-render" not in argv
    plan = load_plan(plan_path)
    out = build_deck(plan)
    print(f"已生成 {out}")
    if do_render:
        render_dir = out.parent / (out.stem + "_render")
        pngs = render(out, render_dir)
        print(f"已渲染 {len(pngs)} 页 → {render_dir}")


if __name__ == "__main__":
    main(sys.argv[1:])
```

- [ ] **Step 2: 删除 workflow.py**

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
git rm skills/pptx-deck/workflow.py
```

- [ ] **Step 3: 写 `tests/pptx_deck/test_build.py`**

```python
"""build.py 单元测试。"""
import json
from pathlib import Path

import pytest

from build import load_plan, load_theme, build_deck, _extract_design_tokens
from themes import tech_blue


def _write_plan(tmp_path, data):
    p = tmp_path / "plan.json"
    p.write_text(json.dumps(data, ensure_ascii=False), encoding="utf-8")
    return p


def test_load_plan_valid(tmp_path):
    p = _write_plan(tmp_path, {
        "theme": "tech_blue", "output": "./out.pptx",
        "slides": [{"layout": "cover", "title": "T", "subtitle": "S"}]})
    plan = load_plan(p)
    assert plan["theme"] == "tech_blue"
    assert plan["_plan_dir"] == str(tmp_path.resolve())


def test_load_plan_missing_field_raises(tmp_path):
    p = _write_plan(tmp_path, {"theme": "tech_blue", "slides": []})
    with pytest.raises(ValueError) as e:
        load_plan(p)
    assert "output" in str(e.value)


def test_load_plan_slide_missing_layout_raises(tmp_path):
    p = _write_plan(tmp_path, {
        "theme": "tech_blue", "output": "./o.pptx",
        "slides": [{"title": "无 layout"}]})
    with pytest.raises(ValueError) as e:
        load_plan(p)
    assert "第 1 页" in str(e.value)


def test_load_theme_tech_blue():
    assert load_theme("tech_blue") is tech_blue


def test_load_theme_unknown_raises():
    with pytest.raises(ValueError):
        load_theme("nope")


def test_load_theme_pptx_missing_raises():
    with pytest.raises(FileNotFoundError):
        load_theme("/tmp/does_not_exist_xyz.pptx")


def test_build_deck_produces_pptx(tmp_path):
    p = _write_plan(tmp_path, {
        "theme": "tech_blue", "output": "./deck.pptx",
        "slides": [
            {"layout": "cover", "title": "标题", "subtitle": "副标题"},
            {"layout": "bullet_list", "title": "要点", "items": ["a", "b"]}]})
    plan = load_plan(p)
    out = build_deck(plan)
    assert out.exists()
    assert out.stat().st_size > 0
    # 相对 output 按 plan 文件目录解析
    assert out.parent == tmp_path.resolve()


def test_build_deck_unknown_layout_raises(tmp_path):
    p = _write_plan(tmp_path, {
        "theme": "tech_blue", "output": "./d.pptx",
        "slides": [{"layout": "nonexistent_xyz"}]})
    plan = load_plan(p)
    with pytest.raises(ValueError) as e:
        build_deck(plan)
    assert "未知 layout" in str(e.value)


def test_build_deck_bad_field_raises_with_page_number(tmp_path):
    p = _write_plan(tmp_path, {
        "theme": "tech_blue", "output": "./d.pptx",
        "slides": [{"layout": "cover", "wrong_field": "x"}]})
    plan = load_plan(p)
    with pytest.raises(ValueError) as e:
        build_deck(plan)
    assert "第 1 页" in str(e.value)
```

- [ ] **Step 4: 删 test_workflow.py,跑测试**

```bash
git rm tests/pptx_deck/test_workflow.py
python3 -m pytest tests/ -v
```

期望：全 PASS（layout 9 + tech_blue 14 + helpers 5 + build 9 = 37）。

- [ ] **Step 5: commit**

```bash
git add skills/pptx-deck/build.py tests/pptx_deck/test_build.py
git commit -m "feat(pptx-deck): replace workflow.py with honest build.py"
```

---

## Task 4：端到端 smoke test

**Files:**
- Create: `skills/pptx-deck/examples/demo_plan.json`（替代旧的 demo_brief.yaml 用途）

- [ ] **Step 1: 写 demo_plan.json**

```json
{
  "theme": "tech_blue",
  "output": "./sample_output.pptx",
  "slides": [
    {"layout": "cover", "title": "iLovePPT v2 演示", "subtitle": "deck_plan 驱动的机械构建"},
    {"layout": "toc", "sections": ["背景", "方案", "效果"]},
    {"layout": "section_divider", "num": 1, "title": "背景"},
    {"layout": "bullet_list", "title": "三个痛点", "items": ["文字墙", "留白多", "改动散"]},
    {"layout": "cards", "title": "三大改进", "cards": [
      {"title": "接缝", "body": "deck_plan.json 隔开机械与智能"},
      {"title": "原语", "body": "layout.py 几何原语,数量灵活"},
      {"title": "回归", "body": "evals 集守护质量"}]},
    {"layout": "compare", "title": "v1 vs v2", "items": [
      {"title": "v1", "body": "骑墙,占位骨架"},
      {"title": "v2", "body": "诚实,机械/智能分离"}]},
    {"layout": "summary", "conclusions": ["接口诚实", "布局可组合", "质量可回归"]},
    {"layout": "closing", "subtitle": "github.com/pcliangx/iLovePPT"}
  ]
}
```

- [ ] **Step 2: 跑 build.py**

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
python3 skills/pptx-deck/build.py skills/pptx-deck/examples/demo_plan.json
```

期望：`已生成 .../examples/sample_output.pptx`、`已渲染 8 页`。

- [ ] **Step 3: 视觉抽检**

Read `skills/pptx-deck/examples/sample_output_render/page-*.jpg` 中的 cover / cards（3 列）/ compare（2 列）/ bullet_list（验证留白居中）/ closing。确认：卡片数对、内容不顶端挤、无重叠溢出、中文不 fallback。若有问题定位回 Task 1/2。

- [ ] **Step 4: commit**

```bash
git add skills/pptx-deck/examples/demo_plan.json skills/pptx-deck/examples/sample_output.pptx
git commit -m "test(pptx-deck): add demo_plan.json end-to-end smoke"
```

注：旧 `demo_brief.yaml` 暂留（Task 9 文档更新时处理）。

---

# Phase C —— Eval 回归集

## Task 5：`evals/` 脚手架

**Files:**
- Create: `evals/rubric.md`、`evals/README.md`、`evals/run_eval.sh`

- [ ] **Step 1: 写 `evals/rubric.md`**

评分标准文档,3 个维度,每项 pass/fail：

```markdown
# iLovePPT Eval 评分标准

对每个 deck 的每一页,按下列检查项打分（pass / fail）。

## 视觉（取自 visual-qa.md 12 项）
1. 无元素重叠　2. 无文字溢出/截断　3. 中文字体未 fallback
4. 标题与内容区间距合理　5. 颜色对比度足够　6. layout 与意图相符
7. 数字/图无 margin 偏移　8. 配色在 BRAND_*/GRAY_* 内
9. 留白边界达标　10. 表格无意外 banding　11. 无 emoji 误用
12. 装饰大字号未异常换行

## 留白与对齐
13. 内容区无大片异常空白（短内容应居中/均布,不顶端挤）
14. 列/行对齐网格

## 结构
15. 页序节奏合理（封面→目录→章节→内容→总结→封底）
16. 图密度合理（若 plan 含 pic_text）

## 打分
每页记录 fail 的项号。一个 deck 的得分 = 总检查数 - 总 fail 数。
```

- [ ] **Step 2: 写 `evals/run_eval.sh`**

```bash
#!/usr/bin/env bash
# 批量跑 evals/plans/*.json → build + render → 出 scorecard 模板
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PLANS_DIR="$ROOT/evals/plans"
OUT_DIR="$ROOT/evals/_run"
rm -rf "$OUT_DIR" && mkdir -p "$OUT_DIR"

SCORECARD="$OUT_DIR/scorecard.md"
echo "# Eval Scorecard（待 Claude 按 rubric.md 填）" > "$SCORECARD"
echo "" >> "$SCORECARD"

for plan in "$PLANS_DIR"/*.json; do
    name="$(basename "$plan" .json)"
    echo "== $name =="
    python3 "$ROOT/skills/pptx-deck/build.py" "$plan" || { echo "  构建失败"; continue; }
    echo "## $name" >> "$SCORECARD"
    echo "渲染图见 evals/plans/${name}_render/。逐页按 rubric.md 记录 fail 项号：" >> "$SCORECARD"
    echo "" >> "$SCORECARD"
done

echo "完成。scorecard 模板：$SCORECARD"
echo "下一步：Claude 用 Read 看各 *_render/page-*.jpg,按 rubric.md 填 scorecard。"
```

- [ ] **Step 3: 写 `evals/README.md`**

```markdown
# iLovePPT Eval 回归集

固定 deck_plan → build → render,检出 layout/build 代码回归。

## 跑
\`\`\`bash
bash evals/run_eval.sh
\`\`\`

## 评分
runner 出 `evals/_run/scorecard.md` 模板。Claude 用 Read 看
`evals/plans/<name>_render/page-*.jpg`,按 `rubric.md` 逐页记 fail 项,填 scorecard。

## 回归判定
新 scorecard 对比 `evals/baseline/scorecard.md`。fail 项变多 = 回归。

## 为什么用固定 plan
eval 隔离"layout/build 代码"的确定性回归,不掺 Claude brief→plan 拓写的不确定性。
```

- [ ] **Step 4: chmod + commit**

```bash
chmod +x evals/run_eval.sh
git add evals/rubric.md evals/README.md evals/run_eval.sh
git commit -m "feat(evals): add eval scaffold (rubric, runner, readme)"
```

---

## Task 6：8 个固定 eval deck_plan

**Files:**
- Create: `evals/plans/01_short.json` … `evals/plans/08_template_extract.json`

- [ ] **Step 1: 写 8 个 deck_plan.json**

每个覆盖 spec §5.2 一个场景。各 plan 的 `output` 用 `./<name>.pptx`（相对 plans/ 目录）。

1. `01_short.json` — 6 页：cover/toc/section_divider/bullet_list/summary/closing
2. `02_long.json` — 20+ 页：多章节循环
3. `03_cards.json` — 含 cards 分别 2/3/4 列各一页
4. `04_compare.json` — 含 compare 2 列、3 列各一页
5. `05_pictext.json` — 含 pic_text，`image_path` 指向一张预放的测试 PNG（用 `evals/plans/_assets/sample.png`，Step 2 生成）
6. `06_table.json` — 多个 table 页
7. `07_chinese.json` — 中文密集，长标题、多要点
8. `08_template_extract.json` — `theme` 指向一个 .pptx 模板路径（用 `evals/plans/_assets/template.pptx`，Step 2 用 build.py 先生成一个充当模板）

每个 plan 的 slides 用 Task 4 demo_plan.json 的格式,内容自拟（覆盖该场景即可,文案不需精致——eval 测的是 layout/build 代码）。

- [ ] **Step 2: 生成 plan 5/8 需要的 assets**

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
mkdir -p evals/plans/_assets
# sample.png：用 Pillow 生成一张 1600x900 占位图
python3 -c "from PIL import Image; Image.new('RGB',(1600,900),'#1E6FE0').save('evals/plans/_assets/sample.png')"
# template.pptx：先用 demo_plan 构建一个,充当 08 的模板输入
python3 skills/pptx-deck/build.py skills/pptx-deck/examples/demo_plan.json --no-render
cp skills/pptx-deck/examples/sample_output.pptx evals/plans/_assets/template.pptx
```

- [ ] **Step 3: 跑 runner 验证 8 个 plan 都能 build**

```bash
bash evals/run_eval.sh
```

期望：8 个 plan 全部 `已生成`，无 `构建失败`。

- [ ] **Step 4: commit**

```bash
git add evals/plans/
git commit -m "feat(evals): add 8 fixed eval deck_plans"
```

---

## Task 7：跑 eval 出 baseline

**Files:**
- Create: `evals/baseline/scorecard.md`

- [ ] **Step 1: 跑 runner**

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
bash evals/run_eval.sh
```

- [ ] **Step 2: Claude 按 rubric 评分**

逐个 `evals/plans/<name>_render/page-*.jpg` 用 Read 查看,对照 `evals/rubric.md` 16 项,逐页记录 fail 项号。把结果填入 `evals/_run/scorecard.md`。

- [ ] **Step 3: 固化为 baseline**

```bash
cp evals/_run/scorecard.md evals/baseline/scorecard.md
```

baseline 即"当前已知良好状态"。今后改 layout/build 代码后重跑 eval,新 scorecard 对比此 baseline——fail 变多即回归。

- [ ] **Step 4: commit**

```bash
git add evals/baseline/scorecard.md
git commit -m "test(evals): record baseline scorecard"
```

---

# Phase D —— 诚实命名与文档

## Task 8：重命名 + workflow.md 重写

**Files:**
- Rename: `skills/pptx-deck/template-ingest.md` → `skills/pptx-deck/template-extract.md`
- Modify: `skills/pptx-deck/workflow.md`（重写为 Claude 流程）

- [ ] **Step 1: 重命名 template-ingest.md 并改写内容**

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
git mv skills/pptx-deck/template-ingest.md skills/pptx-deck/template-extract.md
```

改写 `template-extract.md`：标题改"从模板提取主色与字体"；正文删去"学风格"措辞,如实说明只提取 accent1 主色 + master ea 字体两项;函数名引用改 `_extract_theme_from_pptx`；token 提取失败回退 tech_blue 默认。

- [ ] **Step 2: 重写 workflow.md**

`workflow.md` 改写为 **Claude 执行流程**（不再描述 Python `run()`）：

```markdown
# pptx-deck 主流程（Claude 执行）

端到端：用户 brief → 完整 .pptx。机械部分由 build.py 做,智能部分 Claude 照本文档做。

## 接缝：deck_plan.json
Claude 产出、build.py 消费。schema：{theme, output, slides:[{layout,...}]}。

## 流程
1. 读 brief（自由对话或 brief.yaml），校验有 主题/大纲/输出 意图
2. 图层规划——按 [diagram-planning.md](diagram-planning.md) 判断哪些章节配图；
   需图则先调 [[diagram]] skill 出 PNG
3. 逐页拓写——按 [content-writing.md](content-writing.md) 把大纲扩成 slides 列表，
   配图页 layout=pic_text、image_path 指向上一步的 PNG
4. 写出 deck_plan.json
5. 跑 `python3 build.py deck_plan.json` → .pptx + 渲染 PNG
6. 视觉自检——按 [visual-qa.md](visual-qa.md) 逐页 Read 渲染图,发现问题改
   deck_plan.json,重跑 build.py（至多 3 轮,仍不过标 review-needed）
7. 交付 .pptx

## build.py 能力边界
build.py 只做机械构建（deck_plan → .pptx + PNG）。它不拓写、不规划图、不自检。
那些是 Claude 照文档做的智能步骤。
```

- [ ] **Step 3: 校验无死链**

```bash
grep -rn "workflow.py\|template-ingest\|_ingest_template\|generate_outline\|vision_check" skills/pptx-deck/workflow.md skills/pptx-deck/template-extract.md
```

期望：无输出（旧名全部清除）。

- [ ] **Step 4: commit**

```bash
git add skills/pptx-deck/workflow.md skills/pptx-deck/template-extract.md
git commit -m "docs(pptx-deck): rewrite workflow.md as Claude process, rename template-extract"
```

---

## Task 9：SKILL.md / CLAUDE.md / 子文档更新

**Files:**
- Modify: `skills/pptx-deck/SKILL.md`、`skills/pptx/SKILL.md`、`skills/diagram/SKILL.md`
- Modify: `CLAUDE.md`
- Modify: `skills/pptx-deck/diagram-planning.md`、`visual-qa.md`、`content-writing.md`

- [ ] **Step 1: 更新 pptx-deck/SKILL.md**

- 子文档导航表：`template-ingest.md` → `template-extract.md`；`workflow.md` 描述改"Claude 流程"
- layout 清单：`two_col_compare`/`three_col_cards` → `compare`/`cards`
- 主流程：引用 build.py + deck_plan.json
- 加一行：`[[skill-name]]` 是文档交叉引用约定,非自动解析机制

- [ ] **Step 2: 更新 content-writing.md 的 layout schema**

`compare` schema 改 `{layout, title, items:[{title,body}]}`；`cards` 同理 `{layout, title, cards:[{title,body}]}`。删 `two_col_compare`/`three_col_cards` 旧 schema。

- [ ] **Step 3: 更新 diagram-planning.md / visual-qa.md**

删去"骨架函数 plan_diagrams/vision_check"措辞,改为"Claude 执行步骤"；对接 deck_plan.json（配图页 image_path 在 plan 落地前已生成）。

- [ ] **Step 4: 更新 CLAUDE.md**

- 架构段：`workflow.py` 占位骨架的描述 → `build.py` + `deck_plan.json` 接缝；补"Python 是机械,Claude 供智能"对应 build.py
- SSOT 段：补 `layout.py`（几何原语,主题无关）
- commands 段：`workflow.py` 命令 → `build.py deck_plan.json`

- [ ] **Step 5: 更新 pptx/SKILL.md、diagram/SKILL.md**

`pptx/SKILL.md`：helper 入口补 `layout.py`。`diagram/SKILL.md`：确认与 deck_plan 配图链路描述一致。各加 `[[]]` 约定说明一行。

- [ ] **Step 6: 校验**

```bash
grep -rn "workflow.py\|two_col_compare\|three_col_cards\|_ingest_template\|generate_outline" skills/ CLAUDE.md
```

期望：无输出。

- [ ] **Step 7: commit**

```bash
git add skills/ CLAUDE.md
git commit -m "docs: update SKILL.md/CLAUDE.md/child docs for v2"
```

---

## Task 10：README / USAGE / MANUAL 更新

**Files:**
- Modify: `README.md`、`USAGE.zh.md`、`docs/MANUAL.zh.md`
- Delete: `skills/pptx-deck/examples/demo_brief.yaml`、`brief.example.yaml`（被 demo_plan.json 取代）

- [ ] **Step 1: 决定 brief 文件去留**

v2 的输入接缝是 `deck_plan.json`。`brief.yaml` 是用户给 Claude 的自由输入,不需要固定 schema 文件。删 `demo_brief.yaml` 与 `brief.example.yaml`：

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
git rm skills/pptx-deck/examples/demo_brief.yaml skills/pptx-deck/brief.example.yaml
```

- [ ] **Step 2: 更新 README.md**

- 三 skill 表保留
- 快速开始：`python3 skills/pptx-deck/build.py <deck_plan.json>`（不再是 workflow.py brief.yaml）
- 加 spec §2 的管线图（brief →Claude→ deck_plan.json →build.py→ .pptx）
- 删除/修正"自动配图"等暗示——明确 build.py 只做机械构建,配图/拓写/自检是 Claude 驱动
- 加 evals 段：`bash evals/run_eval.sh`

- [ ] **Step 3: 更新 USAGE.zh.md**

更新到 v2 词汇：build.py / deck_plan.json / `compare`·`cards`。删除 brief.yaml schema 章节,改为"Claude 按 workflow.md 流程从你的需求产出 deck_plan.json"。

- [ ] **Step 4: 更新 MANUAL.zh.md**

逐章核对：第 4 章(第一次使用)→ build.py + deck_plan.json；第 5 章(brief 参考)→ 改为 deck_plan.json schema；第 6 章(11 layout)→ `compare`/`cards` 改名 + 数量灵活；第 7 章(工作流)→ Claude 流程；第 9-10 章→ 命名更新。其余章节核对 workflow.py / two_col_compare 等旧词并替换。

- [ ] **Step 5: 校验**

```bash
grep -rn "workflow.py\|two_col_compare\|three_col_cards\|demo_brief\|brief.example" README.md USAGE.zh.md docs/MANUAL.zh.md
```

期望：无输出（或仅在"v1→v2 变化"类说明性上下文）。

- [ ] **Step 6: commit**

```bash
git add README.md USAGE.zh.md docs/MANUAL.zh.md
git commit -m "docs: update README/USAGE/MANUAL for v2"
```

---

## Task 11：最终一致性校验

- [ ] **Step 1: 全仓旧词扫描**

```bash
cd /Users/pc2026/Documents/DevTools/KillPPTs
grep -rn "workflow.py\|_ingest_template\|template-ingest\|two_col_compare\|three_col_cards\|generate_outline\|vision_check\|fix_slide\|plan_diagrams" \
  --include="*.py" --include="*.md" --include="*.json" --include="*.yaml" . | grep -v ".git/" | grep -v "docs/superpowers/"
```

期望：无输出。`docs/superpowers/`（spec/plan 历史文档）允许保留旧词。逐条修掉非历史文档里的残留。

- [ ] **Step 2: 跑全部测试**

```bash
python3 -m pytest tests/ -v
```

期望：37 PASS（layout 9 + tech_blue 14 + helpers 5 + build 9）。

- [ ] **Step 3: 跑端到端 + eval**

```bash
python3 skills/pptx-deck/build.py skills/pptx-deck/examples/demo_plan.json
bash evals/run_eval.sh
```

期望：demo 构建+渲染成功；8 个 eval plan 全部 build 成功。

- [ ] **Step 4: 交叉引用核对**

```bash
grep -rn "\[\[" skills/ | sort -u
ls skills/pptx-deck/  # 确认 build.py 在、workflow.py 不在、template-extract.md 在
```

确认 `[[pptx]]`/`[[diagram]]`/`[[pptx-deck]]` 引用对应 skill 存在；文件清单符合 v2。

- [ ] **Step 5: commit（若有修正）**

```bash
git add -A
git status --short
git commit -m "docs: v2 final consistency pass"
```

无修正则跳过。

---

## 完成标准

- [ ] 全部 11 任务 commit 在主分支
- [ ] `python3 -m pytest tests/` 全 PASS（37 个）
- [ ] `layout.py` 几何原语有真单测（非 shape-count）
- [ ] `python3 skills/pptx-deck/build.py skills/pptx-deck/examples/demo_plan.json` 跑通,8 页视觉抽检无异常,内容页留白居中、卡片数正确
- [ ] `bash evals/run_eval.sh` 8 个 plan 全 build 成功;`evals/baseline/scorecard.md` 已记录
- [ ] 全仓无 `workflow.py`/`two_col_compare`/`_ingest_template` 等旧词残留（docs/superpowers/ 历史文档除外）
- [ ] `workflow.py` 已删,`build.py` 就位;`deck_plan.json` 作为机械/智能接缝
