# decks/.gitignore — 整目录默认不入 git
#
# 用户的 deck 工作内容(brief / outline / content / state / pptx / 渲染 PNG / raw 素材)
# 体积大,可能含客户数据 / 内部信息 / 版权图,默认不上传 github。
#
# 如需 share 某个 deck:
#   git add -f decks/<name>/<specific-file>     # 显式强加单文件
#   # 或临时挪出 decks/ 目录另外管理
*
!.gitignore
