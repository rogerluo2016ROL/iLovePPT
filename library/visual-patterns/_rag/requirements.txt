# Visual Patterns RAG · Python 依赖(v0.5.4 起 hosted API · 不再需要 torch / sentence-transformers)
#
# 安装(全新 venv):
#   cd library/visual-patterns/_rag
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# API key 配置:
#   把 DASHSCOPE_API_KEY 写到 _rag/.env(gitignored)或 export 到环境

sqlite-vec>=0.1.0               # sqlite 向量扩展(轻量,无需独立服务)
pyyaml>=6.0                     # 读 pattern.yaml

# 注:
# - 不再需要 sentence-transformers / torch(模型托管在阿里云 DashScope)
# - urllib + json 是 stdlib · API 调用无需 requests 包
# - base64 是 stdlib · 本地 PNG 转 data URI 无需额外依赖
